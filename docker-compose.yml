version: '$buildNumber'
services:
  springboot:
    image: ragavandockeraccount/dockercomposefile:$buildNumber
    restart: always
    ports:
    - 9000:8080
    networks:
    - mavenappbridge
networks:
  mavenappbridge:
